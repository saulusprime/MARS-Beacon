# Dipendenze di esecuzione di seo_rrf_audit.py e seo_rrf_gui.py.
requests>=2.28
beautifulsoup4>=4.11
lxml>=4.9

# Facoltative, per il recupero vettoriale con embedding reali:
#   pip install sentence-transformers numpy

# Facoltativa, per il monitoraggio citazioni IA (seo_rrf_citations.py):
#   pip install anthropic
