# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# Ambienti virtuali
venv/
.venv/
mars/
.mars/

# Referti e dati generati a runtime (l'esempio consegnato
# audit_miaweb_rrf.html resta versionato: non corrisponde ai pattern)
report*.html
referto*.json
referto*.txt
referto*.html
storico*.jsonl
citazioni*.jsonl

# I golden file dei renderer sono fixture VERSIONATE della suite
# (tests/test_golden.py), non referti generati a runtime
!tests/golden/referto*

# IDE e sistema
.vscode/
.idea/
.DS_Store

# Utenti e sessioni della GUI (dati locali, mai nel repo)
mars_gui.db

# Fork Lighthouse installato a setup-time da
# tools/update-lighthouse.sh (tag in tools/lighthouse-patches/PIN):
# mai nel repo, si ricostruisce da zero a ogni esecuzione
lighthouse/
